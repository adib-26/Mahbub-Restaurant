server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Verification and reset links open directly from an email, so serve the
  # React app for client-side routes instead of returning an nginx 404.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
